# Frontend (Vercel): URL of the deployed compile backend.
#   e.g. VITE_API_BASE=https://latexifier-api.onrender.com
# Leave empty for local dev — Vite proxies /api to the local server on :3019.
VITE_API_BASE=
